<%= form_for(@exercise_info) do |f| %>
  <% if @exercise_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise_info.errors.count, "error") %> prohibited this exercise_info from being saved:</h2>

      <ul>
      <% @exercise_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise %><br>
    <%= f.text_field :exercise %>
  </div>
  <div class="field">
    <%= f.label :calorie %><br>
    <%= f.number_field :calorie %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
